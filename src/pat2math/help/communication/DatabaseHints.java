package pat2math.help.communication;

public class DatabaseHints {
	/*
	private DatabaseManagerHelp db;
	
	public DatabaseHints() {
		db=new DatabaseManagerHelp("help");
	}
	
	public List<Ajuda> select(String op,int lvl) {
		List<?> h = db.find(op,lvl);
		List<Ajuda> hint= new ArrayList<Ajuda>(h.size());
		for(Object obj: h){
			if (obj instanceof Ajuda) hint.add((Ajuda)obj);
		}
		return hint;
	}
	
	public Hashtable<String, Integer> getMaxLvlOfHints() {
		return db.getMaxLvlOfHints();
	}

	
	public String getContentRelatedToHint(String tipo) {
		return db.getContentRelatedToHint(tipo);
	}
	
	public List<Mensagem> getErrorMessage(){
		return db.getMessageErrors();
	}

}*/
}
